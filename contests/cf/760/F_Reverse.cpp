/*
    Written by: akshitforever
*/

#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
#define int long long int
bool rec(int x, int y) {
    if (x == y) return true;
    if (x < y) {
        
    } else {
        return 
    }
}
int32_t main() {
    int x, y;
    cin>>x>>y;
    rec(x, y);
}